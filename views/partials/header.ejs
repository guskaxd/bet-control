<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Plataforma de Apostas</title>
  <link rel="icon" type="image/x-icon" href="img/favicon-32x32.png">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container-fluid d-flex justify-content-between">
    <div>
      <img style="width: 140px;" src="/img/logo_betcontrol.png">
      <% if (user) { %>
      <ul class="navbar-nav d-inline-flex">
        <li class="nav-item"><a class="nav-link" href="/">Dashboard</a></li>
        <li class="nav-item"><a class="nav-link" href="/contas">Contas</a></li>
        <li class="nav-item"><a class="nav-link" href="/lancamentos">Lançamentos</a></li>

  <% if (user && user.tipo_usuario === 'admin') { %>
    <li class="nav-item"><a class="nav-link" href="/admin/painel">Admin</a></li>
  <% } %>
      </ul>
      <% } %>
    </div>
    <% if (user) { %>
    <div>
      <span class="text-white me-2"><i class="bi bi-person-circle"></i> <%= user.email %></span>
      <a href="/logout" class="btn btn-outline-light btn-sm">Sair</a>
    </div>
    <% } %>
  </div>
</nav>
<div class="container">
